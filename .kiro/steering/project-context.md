# HarvestGuard — project memory

The canonical project memory lives in `CLAUDE.md` at the repo root and is
included below. It is the single source of truth for repo layout, data
pipeline decisions, and known gotchas. Keep editing `CLAUDE.md` — this
steering file just pulls it into context so Kiro and the Claude extension
share one memory file instead of drifting apart.

Project pitch and motivation: `description.md`.

#[[file:CLAUDE.md]]
